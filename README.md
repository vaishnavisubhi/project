# project
The app can be accessed using the link https://chat-on-video.netlify.app/
The backend code is done using socket.io in the index.js file whereas the frontend code which uses webRtc and peer to peer connection are done in the client folder.
Deployed the app on heroku cloud application platform 
